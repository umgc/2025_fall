# Auth Domains (Demo Scope)
- Public: GET /health
- Authenticated (PATIENT/CAREGIVER): GET /auth/me, POST/GET /notes, POST /triggers/propose, POST /pii/sanitize
- Future: Provider/Admin domains for advanced RBAC and policy actions.
